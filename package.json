{
  "name": "the-runner",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "qa:smoke": "npm run build && node ./scripts/smoke-preview.mjs",
    "qa:release": "npm run qa:smoke && node ./scripts/release-sanity.mjs && node ./scripts/gameplay-flow-sanity.mjs"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  },
  "dependencies": {
    "three": "^0.160.0",
    "cannon-es": "^0.20.0"
  }
}
